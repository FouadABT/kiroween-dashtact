});
1);
  ess.exit(    procror);
 er error:',ror('Fatalsole.er con => {
   rror) .catch((eds()
 utPageI
fixLayohe script

// Run tt();
  }
}ecsma.$disconn   await priinally {
 r;
  } fthrow erroor);
    eIds:', err pagg layoutError fixinror('❌ sole.er {
    conr)rro } catch (e
 ully!');essfixed succgeIds f✅ Layout pa'\n.log(sole   con
 ;
   })ts)`);
 s} widgestancedgetIn._count.wiayoutame} (${layout.neId}: ${layout.pag- ${log(`    console.l=> {
    layout) ach((s.forEatedLayout);
    upd:` pageIds correctuts withngth} layoyouts.le ${updatedLa\nFoundog(`   console.l });

 
   },        },
       },
        es: true,
 nstancdgetIwi            {
 ect:    sel    ount: {
        _c: true,
   scope       e: true,
nam  rue,
       teId:ag
        p, true      id:
  elect: {      s
},  ,
      }GS),
      PPINAGE_ID_MA(Pt.values   in: Objec     eId: {
    pag      : {
    whereMany({
  .finddLayoutashboarsma.d pri = awaitedLayoutsconst updat  );
  ation:'ficVerie.log('\n📊 onsoles
    c updatVerify the// 
       }

     }
  ;`)\nfullycess Updated sucle.log(`   ✅nso        co    
       });
},
     d d: newPageIgeI { paata: d        
 .id }, layoutd:e: { i        wherpdate({
  yout.uboardLama.dashait pris        awhe pageId
te t/ Upda     /else {
        } n`);
 old layout\ed  ✅ Deletsole.log(`   con  
       
         });,
      layout.id }{ id:e:       wher{
    delete(dLayout..dashboarrismait p      awastances)
  widget inlete  will deout (cascade laylete the old      // De   
  );
     d}'...`{oldPageIageId '$th playout wieleting old `   De.log(onsol    c  sts!`);
  exilready geId}' aewPapageId '${nh wit ⚠️  Layout `  nsole.log(    cot) {
    ingLayouistex (     if });

        },

      out.scope,ay    scope: l      
d,layout.userIId: ser u        PageId,
 ew pageId: n        here: {
     wrst({
    .findFiayoutoardLshba.daprismit Layout = awaistingt ex
      consistsady exalrepageId he new out with tlay if a     // Check

  length}`);nstances.t.widgetI${layou Widgets: sole.log(`       conId}`);
 ewPage{npageId: $ New sole.log(`       cond}`);
 ldPageI{od pageId: $og(`   Ol.l    console}`);
  .name{layoutme: $(`   Na console.log
     yout.id}`);: ${la.log(`   IDconsole`);
       layout:ating Upde.log(`📝   consol
       }
ue;
   contin;
       d`)g founppin no maId}) -ge${oldPad: d} (pageIt.i ${layououtping laykip️  Snsole.log(`⏭ co{
       ewPageId)  if (!n

     geId];PaGS[oldPPIN_MA = PAGE_IDageId  const newP   
 ut.pageId;ageId = layoconst oldP
      pdate) {utsToUout of layoonst lay
    for (clayouth pdate eac    // U:\n`);

tepdalayouts to uh} Update.lengtayoutsTo${l.log(`Found 
    console });
,
   
      }: true,Instanceset   widg {
     nclude:      i,
,
      }
        }INGS),D_MAPPGE_Ikeys(PAbject.: O    ind: {
            pageIhere: {
     wany({
   Layout.findMa.dashboard prismwaitoUpdate = a layoutsTonstating
    c need updouts thatll lay// Get a    try {
  \n');

ageIds...ut payo lg dashboard'🔧 Fixinsole.log(s() {
  conageIdutPn fixLayonc functio;

asyoard',
}erce-dashb': 'ecommmmerce',
  'ecoboardytics-dashalanics': ''analytoard',
  main-dashb 'w':ervie
  'ovPINGS = {GE_ID_MAPPAst 
conlient();
w PrismaCisma = ne

const pr/client');@prisma require('ent } =maClit { Pris
cons
 */fiers.
nti pageIdeem'sstthe menu sytch
 * ds that marect pageIto use corard layouts  dashboxistingdates e
 * Uppt
 * riPageIds Scyout  Laix
 * F/**